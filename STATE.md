# Где мы находимся

Записка для продолжения работы «с чистого листа»: что уже сделано, чем это проверяется
и что осталось. Обновляется в конце крупных этапов.

Последнее обновление: раунд «свет, текстуры, база, perf» — по скриншоту заказчика.
Репозиторий: https://github.com/Rxd-essss/dm-duel — **всё запушено**, дерево чистое.

---

## 1. Что это за проект

Снайперский аркадный шутер на three.js r128. Собирается из модулей `src/*` в **один
самодостаточный HTML** — `dm_duel_v3.html`, который просто открывается в браузере.
Есть мультиплеер до 4 игроков на собственном сервере без зависимостей.

| Файл | О чём |
|---|---|
| [CONTRACT.md](CONTRACT.md) | правила модулей, владение файлами, публичный API |
| [NETCONTRACT.md](NETCONTRACT.md) | протокол мультиплеера и модель авторитета |
| [MAPDESIGN.md](MAPDESIGN.md) | планировка; **действующий раздел §10**, §1–§9 архив |
| [README.md](README.md) | как играть и как запускать |

---

## 2. Команды

```bash
node build.js              # собрать src/* -> dm_duel_v3.html + проверить синтаксис и дубли имён
node tools/serve.js        # локальный сервер на 8177 (для проверок в браузере)
node net/server.js         # игровой сервер комнаты (он же раздаёт игру)
node tools/mptest.js       # сквозной тест мультиплеера, эталон — 34 из 34
```

В консоли браузера на открытой игре:

```js
(function(){var x=new XMLHttpRequest();x.open('GET','/tools/smoke.js',false);x.send();return eval(x.responseText);})()
(function(){var x=new XMLHttpRequest();x.open('GET','/tools/mapcheck.js',false);x.send();return eval(x.responseText);})()
```

---

## 3. Состояние на сейчас

`smoke` — ok. `mapcheck` — ok, застреваний **0** из 21600 проб (одно давнее предупреждение:
тросов 2 при норме 4). `mptest` — **34 из 34**. Бюджет: 666 мешей, 82 тыс. треугольников
при потолке 700 / 220 тыс.

### Карта «Лесопилка» (MAPDESIGN §10)

Закрытый деревянный зал 44 × 134 × 22 м по референсу заказчика (пользовательская карта
DUEL2 для TF2; геометрия оригинальная, чужой уровень не копировался). Четыре яруса на
канонических высотах **0 / 6 / 12 / 18** м: пол, галереи кольцом, мостки через шахту,
стропила без перил. Торец до торца 129 м, просадка матчевой пули на предельной линии
0.87 м — баллистика работает. 74 позиции, 10 рамп, 11 лестниц, 2 троса.

### Оружие: два ствола

`game.weapon` (0 винтовка, 1 лук). `AMMO` — **тот же массив** на всю игру, содержимое
подменяется на месте через `setWeapon()`; поэтому ~50 мест, читающих `AMMO[i]`, работают
без правок. Таблицы `RIFLE_AMMO`, `BOW_AMMO`, `WPNS` в `src/10_core.js`.

**Лук:** натяг ЛКМ (тянуть — отпустить), оптики нет. Разброс 10.12° при нулевом натяге →
**ровно 0.000° начиная с 0.97** (`WPNS[1].drawTrue`). Просадка на 100 м при полном натяге:
стрела 5.24 м, взрывная 10.92 м, огненная 7.38 м. **СКМ** — залп: 3 стрелы, 3 из колчана,
откат площадных ×3 (взрывная 7.5 с, огненная 15 с). **ПКМ** — приближение 30% и дуга
полёта той же математикой, что реальный полёт (расхождение 0.1–1.1 см); лук уводится
вниз-влево, центральная зона кадра **0.0%**.

### Свет и палитра (раунд по скриншоту)

Заказчик прислал кадр: «карта перенасыщена яркостью, всё засвечено, лучи ужасные и
беруться из ниоткуда, всё однообразно». Разобрано на три причины, каждая своим лекарством.

**Лучи удалены совсем** (`TER_makeShafts` вырезана из `25_terrain.js`). Приглушение их не
спасало: в закрытом зале проём под кровлей в кадр не попадает, и конус висит без видимого
источника. Они же были самой дорогой вещью кадра — 27 тыс. м² двусторонней прозрачности.

**Свет перебран: заливка вниз, направленное вверх.** Было — полусфера 1.40, две встречные
контровые 1.20 и 0.86, непрямой ×1.26…1.34, солнце 0.37. Две контровые с противоположных
боков взаимно гасят направление, то есть работали третьей заливкой, вчетверо сильнее
солнца. Стало: полусфера 1.15 (обе половины сближены — в зале «неба сверху» нет),
контровые 0.42 и 0.16, непрямой 1.05…1.04, **солнце 0.60 и оно единственное с тенью**.
Рамка тени ±44 м и едет за ВЗГЛЯДОМ (`85_player.js`), а не только за игроком.
Экспозиция 6.40, лампы `LIGHTS.mul` 2.60 — они несут нижние ярусы.
Дымка из тёплого песка `0xc0aa8b` уведена в холодный серый `0x9aa0a6`: она мешается ПОСЛЕ
тонмаппинга, то есть красила всю дальнюю половину зала в оранжевый.

**Палитра разведена по породе.** Двенадцать материалов оболочки различались только
светлотой при одном оттенке — по красному каналу между самым тёмным брусом и самым светлым
настилом было 8 единиц из 255. Теперь: серо-бурая обшивка (`wall*`), почти чёрный каркас
(`beam*`), тёплые настилы (`deck*`), холодные железо/кровля/земля, бетон и кладка на базах.

**Текстуры**: доска и земля перерисованы (пять неравных досок, гвозди, следы пилорамы,
вытертость; на земле — глина, щепа, колеи, масляные пятна), дереву и грунту поднято
разрешение до 512 (`TEX.big`), у `vplank` своё зерно — стена и настил больше не несут один
рисунок. **Крупных мягких пятен внутри плитки нет ни у доски, ни у земли, и это решение.** При
повторе 7 × 21 на пол 44 × 134 м пятно возвращается каждые шесть метров, и на скриншоте
пол читался леопардовым — тёмные кляксы правильной решёткой. Сперва их приняли за тень от
кровли (крышу даже заставили тень бросать — это осталось, так честнее), но узор держался,
потому что нарисован в плитке. Крупная неоднородность берётся из РАЗНЫХ поверхностей:
дорожки вдоль створов, круги опилок, бетон базы.

Замер по восьми ракурсам игрока: средняя **123.9**, темнее 60 — **26.8%**, светлее 235 —
**1.1%**. Рамка приёмки 120…160 / ≥6% / 0.5…4%.

### Раунд «сумрачный цех» — по трём претензиям заказчика

Заказчик забраковал предыдущий результат словами «с освещением как был пиздец так и
остался, с текстурами и продуманностью карты такой же, снайперка визуально сломалась».
Третий пункт оказался ключом ко всем трём.

**ВИНТОВКУ УДАЛЯЛА МОЯ ЖЕ ЗАЩИТА.** В  стояла «чистка модели», которая
выкидывала меши крупнее 0.6 м с белым материалом — по догадке, что деталь такого размера
в руках существовать не может. Под это описание попала САМА ВИНТОВКА:  собирает
модель через , тот сливает сотню коробок в несколько мешей и переносит цвет в
ВЕРШИНЫ, поэтому у слитого меша материал по построению белый, а габарит — габарит всей
винтовки (1.17…1.72 м). После чистки в руках оставались два кружка линз и кисти рук.
Защита удалена.

**«2 больших синих куба» — это были РУКАВА.** Предплечья стояли на +0.24 по Y, то есть
поднимались от кисти ВВЕРХ, и рукав 13.5 см оказывался в двадцати сантиметрах от глаза
посреди кадра. К модели винтовки они отношения не имели. Руки развёрнуты вниз-назад.

**У ОРУЖИЯ ТЕПЕРЬ СВОЯ ЭКСПОЗИЦИЯ** (, ). Сцена вида
освещена своим ригом, а тонмаппинг был общий с миром: когда мировая экспозиция уехала с 2
на 6.4, оружие выгорело вместе с ней. Любая правка  теперь требует взгляда
на кадр С ОРУЖИЕМ — замеры мира этого не показывают в принципе.

**СВЕТ ВЫБРАН ЗАКАЗЧИКОМ ИЗ ТРЁХ ВАРИАНТОВ** — «сумрачный цех»: солнце 0.34 холодное,
полусфера 0.85, лампы  3.6 несут нижние ярусы тёплыми лужами, дымка
0x6f7a85 с 20 до 190 м, экспозиция 6.80. Рамка яркости пересмотрена: **средняя 75…95,
темнее 60 — 40…55%, светлее 235 — до 1.5%** (было 120…160). Замер: 83.8 / 47.7% / 0.64%.
Отрыв силуэта цели от фона на 100 м — 13 единиц.

**ПОРОДЫ ПОВЕРХНОСТЕЙ** (жалоба «всё дерево на одно лицо»): добавлена процедурная кладка
 (перевязка, разброс по кирпичам, шов, выщербины, высолы) и шесть материалов —
кирпич трёх тонов, гофролист, ржавый лист, бетон, прокат. В карте: кирпичный цоколь по
всему периметру, чередование доски и гофролиста по пролётам обшивки со стальными
стойками по швам, стальные фермы кровли,  — котлы с обмуровкой,
бетонные основания и паровые магистрали в полосе под галереями.

**РЕЗКОСТЬ ВБЛИЗИ** (жалоба «текстуры мыльные»): дереву, земле и камню поднято
разрешение до 1024 (), анизотропия с 4 до 16 — именно она решает мыло на полу
и настилах, которые видно под острым углом.

Потолок мешей в  поднят с 700 до 900: каждый новый материал стоит около трёх
мешей на зал, а настоящий бюджет — треугольники (86 тыс. из 220 тыс.).

### База и лестницы

`SAW_dressBase` в `45_map.js`: бетонный пол базы, цоколь и контрфорсы кладкой, рама ворот
с косынками и командной обвязкой, полоса командного цвета по стенам гнезда, круг разметки
на полу, нащельники проёмов, мешки у выхода, фонари на ригеле.
`mkStair` в `40_props.js`: появились поручень и тяга (раньше стояли столбики перил БЕЗ
перил), носок проступи, опорная стойка под серединой марша, косоуров 4–8 звеньев вместо 3.
Подступёнка нет намеренно — марш сквозной, и сплошная с виду доска, через которую летит
пуля, врала бы игроку (§0.9).

### Производительность

Жалоба «сильно стало лагать» появилась вместе с луком, и это не совпадение: `rayBoxes`
перебирал все 444 коробки карты, а у снаряда четыре вызова за кадр. Стрела медленнее пули
и стреляют ею чаще — снарядов в воздухе стало на порядок больше.

Сделана **широкая фаза**: равномерная сетка 9 м по XZ, луч идёт по ней алгоритмом DDA
(`PHYS_buildGrid`, `PHYS_G` в `30_physics.js`; строится в `boot()` после `buildMap`).
Внутренний цикл заодно развёрнут по осям — он создавал три временных массива на КАЖДУЮ
коробку.

| что | было | стало | быстрее |
|---|---|---|---|
| подшаг стрелы (1.2 м) | 464 мс | 32 мс | **14.5×** |
| подшаг пули (5 м) | 370 мс | 37 мс | 10× |
| выстрел (60 м) | 420 мс | 75 мс | 5.6× |
| прострел зала (130 м) | 411 мс | 79 мс | 5.2× |

*(50 тыс. лучей на замер.)* Совпадение с полным перебором проверено: **0 расхождений на
20 000 случайных лучей**, включая нормаль попадания.

Плюс к этому: ландшафт 28.8 → 6.3 тыс. треугольников (`TER_SEG` 120 → 56; `terrainH`
аналитическая и правку не замечает), облака 9.9 → 2.2 тыс., лучи убраны. По кадру:
26 тыс. треугольников там, где было 56 тыс.
И **ступень качества наконец доступна**: код `setQuality` искал `#optQ`, а ручки в разметке
не было — плотность пикселей навсегда стояла 1.5. Ползунок добавлен в настройки.

### Прочее

Паркур, выбор стороны BLU/RED с динамической командой ботов, полноэкранный режим (`F`),
барьеры баз (односторонние по команде), динамический свет пулом из 8 ламп, процедурные
текстуры, мультиплеер с авторитетным сервером.

---

## 4. Что осталось незакрытым

1. **Корень бага с белыми плитами не устранён.** В `mkRifle` (`src/50_models.js`) строятся
   четыре меша габаритом 1.17–1.72 м с белым материалом — заказчик видел их как «2 больших
   синих куба». Сейчас они выкидываются защитой в `buildViewmodel` (`src/60_weapon.js`) по
   двум признакам: негабаритная И некрашеная. Это заплатка; найти и починить сам сборщик.
2. **Боты доходят до середины лишь частично** — за 25 с двое из четырёх пересекают z=44,
   и это не изменилось после отделки базы (проверено: столько же было и до неё). Причина
   найдена и записана здесь, чтобы не искать заново: **у трёхэтажного гнезда появления нет
   ни одной внутренней связи между этажами**. Боец, появившийся на ярусе 2, упирается в
   боковую стену рядом с дверью и стоит — сойти вниз ему негде, а обход через пандус на
   другой стороне двора ИИ не находит. Лечится геометрией: лестница или лаз внутри гнезда
   (нужен люк в плите яруса 1, она же торцевая галерея) либо балкон снаружи дверей.
3. Ощущение лука (`WPNS[1].drawTime` = 1.05 с, штраф за недотяг) ждёт живой обкатки.
4. **Мелочи по картинке, замеченные на скриншотах, но не поправленные:** знамя базы —
   плоский прямоугольник без складок; ракурс «ярус 3, взгляд вниз» даёт 9.4% пикселей
   ярче 235 при рамке 4% (настилы стропил смотрят вверх и ловят и небо, и солнце);
   база стала заметно темнее зала — это намеренно (она читается
   отдельным помещением), но может оказаться темновато на глаз.

---

## 5. Ловушки этой среды

* Панель браузера скрыта: `renderer.render` в экранный буфер **зависает**. Только offscreen
  `WebGLRenderTarget`, `loop()` с заглушенным `requestAnimationFrame`.
* `const`/`let` верхнего уровня **не попадают в `window`**: проверять через
  `new Function('return typeof X !== "undefined"')()`.
* Правки в живой странице (`CFG.step`, `RND_setExposure`) переживают повторные вызовы —
  после экспериментов страницу перезагружать, иначе замеры врут.
* Сервер валидирует заявки по-настоящему: тестовый клиент обязан стрелять из своей позиции,
  целиться в жертву, выдерживать темп и брать пикап стоя рядом.
* Таблицы точек респавна и предметов в `net/server.js` — **копия карты**. При правке
  `45_map.js` их надо обновлять, иначе в сети отвалятся пикапы.
* **Первый выстрел после `startGame()` в тестовой обвязке стабильно теряется** — прогревать
  холостым выстрелом, иначе замер покажет «0 стрел» на исправном оружии.
* **Смотреть на кадр можно, панель для этого не нужна.** Рабочая схема: свой сервер отдаёт
  игру и принимает `POST /shot` с `canvas.toDataURL()`, страница рисует в offscreen
  `WebGLRenderTarget`, переворачивает строки (`readRenderTargetPixels` отдаёт снизу вверх)
  и шлёт PNG на диск. Дальше картинку просто открываешь. Это единственный способ поймать
  то, что не ловят замеры, — см. §6.
* **Полнокадровый замер времени в этой обвязке шумит** ±25% от прогона к прогону: `loop()`
  вызывается вручную, GPU не синхронизирован. Сравнивать варианты только изолированным
  бенчмарком (как сделан замер `rayBoxes`), а не медианой кадра.
* **`TX_expose` возводит текстуру в степень 3.6** (`TX_TONE.con` у крупных пород). Любая
  вариация, заложенная в генератор, растягивается почти вчетверо: разброс тона по доскам
  ±6% дал на стене розовые доски вперемешку с зелёными. Рабочий разброс — ±0.8%.

---

## 6. Главный урок этой работы

**Замеры по пикселям не заменяют взгляд на картинку.** Три жалобы заказчика подряд —
пересвет, запертые боты, синие кубы — мои проверки пропустили, потому что мерили среднюю
яркость и долю выбитых пикселей. Эти метрики не ловят ни белую плиту поперёк экрана, ни
луч, идущий сквозь балку. **Один скриншот заказчика дал больше, чем три раунда замеров.**

Отсюда правило: при жалобе на внешний вид — просить скриншот, а не спорить числами.
И проверять то, что видит игрок (ракурсы с ярусов, из прицела), а не медиану по карте.
